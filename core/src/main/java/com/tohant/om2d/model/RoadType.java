package com.tohant.om2d.model;

/**
 * Road type. Used to determine which road texture to use.
 * E.g. {@link RoadType.TOP} is a road with cross line on the top of road texture.
 */
public enum RoadType {
    LEFT, RIGHT, TOP, BOTTOM, EMPTY
}
